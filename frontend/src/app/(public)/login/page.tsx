import { RouteStub } from "@/src/shared/layouts/RouteStub";

export default function LoginPage() {
  return (
    <RouteStub
      title="Login"
      description="Authentication portal — awaiting Figma/Stitch implementation."
    />
  );
}
